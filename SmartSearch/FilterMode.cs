namespace WPFCommons.SmartSearch
{
    /// <summary>
    ///   Indicate filter mode configuration (AND || OR)
    /// </summary>
    public enum FilterMode
    {
        AND,
        OR,
    }
}